import { PlaceholderPanel } from "@/components/placeholder-panel";

export default function SignupPage() {
  return (
    <PlaceholderPanel
      title="Signup route shell"
      description="This page is reserved for the auth flow. The initial implementation keeps it intentionally minimal while the registry model, versions, and raw download path are established."
    />
  );
}

