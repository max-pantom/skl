import { PlaceholderPanel } from "@/components/placeholder-panel";

export default function SettingsPage() {
  return (
    <PlaceholderPanel
      title="Settings route shell"
      description="Profile edits, avatar management, and connected-auth settings can be added here once the user account flow is wired."
    />
  );
}

