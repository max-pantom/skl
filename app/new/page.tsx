import { PlaceholderPanel } from "@/components/placeholder-panel";

export default function NewSkillPage() {
  return (
    <PlaceholderPanel
      title="Publish flow shell"
      description="The publish route exists now so the form can be wired directly to the skill and skill_version tables in the next pass. For this foundation pass, the schema, migrations, seeded examples, and read paths take priority."
    />
  );
}

