import { PlaceholderPanel } from "@/components/placeholder-panel";

export default function LoginPage() {
  return (
    <PlaceholderPanel
      title="Login route shell"
      description="Authentication is part of the MVP, but this pass focuses on the app shell and core data model first. The route is in place so Better Auth or Supabase Auth can land without restructuring the app."
    />
  );
}

